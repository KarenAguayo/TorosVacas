function CompararToro(codigo,codigoadivina)
{
        let toro = '!';
       if (codigo.charAt(0) === codigoadivina.charAt(0))
        {
            return toro;
        }
}  
export default CompararToro;